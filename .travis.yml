language: generic 
sudo: required
services: 
  - docker

before-install:
  - docker build -t rohitraja56/docker-reacct -f Dockerfile.dev .

script:
  - docker run -e CI=true rohitraja56/docker-react npm run test